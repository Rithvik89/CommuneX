//This prisma schema is specific for SQL Database

generator client {
  provider = "prisma-client-js"
  output   = "./generated/client_mysql"
}


datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
